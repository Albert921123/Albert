---
name: generate-daily-industry-brief
description: Configure, deploy, update, catch up, and generate a source-linked Chinese construction-and-adjacent-industry leadership brief as a standalone searchable HTML file. Supports 16 standard sections, broad custom interests, persisted settings, a 24-hour primary and per-section 48-hour fallback window, multi-route source verification, cross-section evidence cards, retrieval audits, HTML validation, and Asia/Shanghai schedule verification. Use for 知讯日报 setup or generation, 修改板块, 修改推送时间, 打开设置, missed-run catch-up, HTML layout repair, retrieval optimization, or recurring-task repair. Also use for bare invocations such as 调用知讯日报 or 调用日报; open the full selector immediately, or use the complete numbered text selector on a headless host.
---

# Generate Daily Industry Brief

Produce a concise, source-linked Chinese morning brief from the user's selected sections. Deliver the brief as a complete `.html` artifact, not Markdown.

## Full-package reading gate — mandatory before any execution

An Agent must read the **entire installed skill bundle**, not only `SKILL.md`, before it opens a selector, searches, fetches a page, uses a cached candidate, writes HTML, validates an artifact, creates/updates a schedule, or answers that a run has begun. This is a hard precondition for every invocation, including a bare `调用`, a scheduled heartbeat, a catch-up, a layout repair and a settings edit.

1. First run `python scripts/verify_install.py` against the actual installed directory. If it fails, stop; do not execute a partial bundle.
2. Run `python scripts/create_reading_receipt.py --skill-dir <installed-skill-dir> --run-id <unique-run-id> --output <workspace>/.zhixun-reading-<unique-run-id>.json` only after reading every textual installation file listed by that receipt to EOF: root instructions/metadata, `agents/`, every `assets/` template, every `entrypoints/` file, every `references/` file, and every executable source/wrapper in `scripts/`. Binary caches and `__pycache__` are excluded; no other installation content may be skipped.
3. Immediately run `python scripts/verify_reading_receipt.py --skill-dir <installed-skill-dir> --run-id <unique-run-id> --receipt <receipt-path>`. Record the verified receipt path and file count under `run.full_package_reading` in the retrieval ledger.
4. If the receipt is absent, stale, mismatches a file hash, or the Agent cannot read a file to EOF, stop and report `技能包未完整读取，未开始执行`. Do not substitute a summary, a directory listing, a previous run's receipt, or a claim that “the relevant files were read.” A new invocation needs a new run ID and a new receipt after the full package is read.
5. The completion gate applies to every selected board. Reading a source-map excerpt, checking one or two boards, or reading only the template never authorizes partial execution. Do not start board retrieval until the full-package receipt has passed.

## Non-negotiable live-run completion gate

When a live network route is working, create `retrieval-ledger-YYYY-MM-DD.json` before writing any report card and finish the retrieval passes for **every** selected row. Do not emit an early “补发版”、preview, or baseline-filled HTML merely because a few convenient candidates were verified. A live row may end only as `complete`、`observed`、`expanded`、`business-observation` or evidence-backed `checked-empty`; `baseline` is offline-only and is invalid whenever the run audit records an available live route.

If the Agent cannot finish the full candidate ledger in the active run because of an interruption, exhausted execution budget, or unavailable required route, stop and report the unfinished rows instead of publishing a report that looks complete. On the next invocation, restart the unfinished rows with fresh live discovery; do not reuse a partially written HTML as the candidate pool. A human-readable delivery is permitted only after the paired ledger and HTML both pass their validators. The one-event/one-section assignment rule below remains mandatory.

## Verify installation and runtime capabilities

1. A ZIP attachment that an Agent can inspect is not automatically a persistent installation. On first use from a newly received package, preserve the complete directory structure, place the `generate-daily-industry-brief` directory in that host's persistent skill directory, and run `python scripts/verify_install.py`. Do not claim installation when the files exist only in an upload cache, temporary extraction directory, or current conversation.
2. On first use on a new host, after a host upgrade, or whenever `webSearch` is unavailable, read `references/runtime-compatibility.md`, `references/network-retrieval-playbook.md`, `references/role-profiles.md`, `references/coverage-continuity.md`, and `references/macro-editorial-architecture.md`; select the highest verified retrieval mode that the host actually supports. Python is optional: absence of Python must not prevent static configuration, HTML generation, or transparent manual validation.
3. Search capability is not determined by the tool's brand or by the presence of a tool literally named `webSearch`/`webFetch`. Run one read-only capability preflight, record each route as `working`, `skipped-unavailable`, `denied`, or `failed`, and select one working primary transport before retrieval. First inspect the host tool list for any exposed search API/tool (for example `webSearch`, `search_web`, `internet_search`, `browser.search`, knowledge/news search, a configured Bing/Google/SerpAPI-style connector, or another documented search-result API); use the strongest declared working search API for breadth-first discovery. Never invent an endpoint, API key, connector name, or credential that the host did not expose. An unavailable optional tool is not a run error and must be skipped rather than called again. If no search API is exposed or it fails its one harmless probe, continue in order with a controllable local or personal-cloud browser/desktop search page or site-native search, direct HTTP, RSS/sitemaps/listings/public APIs, then a validated external feed. A user-authorized personal cloud computer is a Mode B browser transport: when the Agent can operate it, it must perform the searches itself rather than asking the user to copy queries or results. Probe search API, controllable browser (including user-authorized personal cloud desktops), browser search-page navigation, shell/HTTP client, native HTTP library, RSS/sitemap/listing access, external-feed input, scheduler, and artifact-output capabilities. Record the actual working transport route(s), skipped routes, selected search API (if any), cloud-browser use (if any), and selected retrieval mode in the run audit.
4. When outbound network access is confirmed, it is mandatory to use a live retrieval route (Mode A or B). A verified external feed is an optional coverage enhancement, never the default substitute merely because a named search tool is absent. Only if no live route and no current verified structured feed is available may the run be limited or fail closed as defined in `references/runtime-compatibility.md`; never invent or recall news from model memory. A host cannot claim a complete realtime report when its preflight has no working live route or validated feed.

## Hard route for every settings change

Treat `修改板块`, `调整板块`, `重新选择板块`, `增加或删除板块`, `修改时间`, `调整推送`, `修改自定义兴趣`, and equivalent requests as direct selector-open commands.

1. Immediately open the same complete interactive selector used for first-time setup. Do not ask what to add or remove before opening it.
2. When the host can present the selector, never replace it with `request_user_input`, a host-native multiple-choice/confirmation card, numbered text options, or a manually typed list of changes. When it cannot present a user-accessible selector, follow `Text selector for headless or cloud hosts` instead.
3. On any local host without inline HTML callbacks, the first execution step must be running the selector bridge: on Windows run `scripts\launch_selector.cmd --edit --timeout 900`, on macOS/Linux run `scripts/launch_selector.sh --edit --timeout 900`. The wrappers automatically locate a Python 3.6+ interpreter; invoke `scripts/launch_selector.py` directly only after confirming the interpreter is Python 3.6 or newer. Run the bridge in the foreground as a blocking call with a timeout of at least 900 seconds, and keep the turn running until the browser form is submitted. Process exit is the "user has submitted" signal: the moment the command returns, immediately post one short confirmation in the conversation (for example `已收到你的选择，正在更新订阅任务…`) before parsing and executing anything else, and finish the whole update within the same turn without asking the user for further confirmation. If the host can only run the command in the background, poll its output at least every 5 seconds and continue the instant `ZHIXUN_CONFIG=` appears. Never end the turn while the bridge is still running, and never leave a `完成后告诉我一声`-style wait as the primary path; only when the host forcibly terminates the turn may the handoff pause, and in that case tell the user to send `继续部署` afterwards and follow `Resume an interrupted handoff`.
4. Restore all saved standard sections, custom interests, cadence, delivery time, and delivery channel in the form. The user edits by clicking the existing selections and then presses `保存修改并更新原任务`.
5. If local preferences are missing, silently hydrate them from the single readable existing 知讯日报 automation and pass that JSON with `--initial-config-file`. Ask for manual values only when neither saved preferences nor a readable existing task exists.
6. After submission, update and verify the original automation. Opening the selector is mandatory even when the user's request mentions only one small change.

## Start interactive setup

Treat a bare invocation of this skill — `调用`, `调用知讯日报`, `调用日报`, `使用知讯日报`, `启用知讯日报`, or an equivalent one-word summon — as a first-time setup request and open the selector immediately without asking what the user means.

When the user asks to set up, subscribe, choose sections, open the selector, or create recurring delivery without providing a configuration:

1. Prefer an in-conversation interactive selector when the host supports HTML visualizations and `window.openai.sendFollowUpMessage`. The selector must first expose the four profiles `经营岗`、`生产岗`、`研究岗`、`自定义` and show every standard section's 1–10 relevance score; profile presets recommend high-score sections but never hide or prohibit the other 16 sections.
2. Copy `assets/codex-subscription-selector.html` into the host's writable thread visualization directory and render it as an inline visualization. Do not rewrite or simplify the selector. If a saved or automation-derived configuration is available, inject one JSON-safe assignment, `window.__ZHIXUN_INITIAL_CONFIG__ = <normalized JSON>`, before the selector script so the form restores it; the asset also retains the last submitted configuration in local storage as a fallback. Never inject unescaped user text as HTML.
3. Treat the selector's primary action as authorization to create the recurring task. Do not ask for the same confirmation again.
4. On a local host without inline HTML callbacks, run the selector bridge: on Windows run `scripts\launch_selector.cmd --timeout 900`, on macOS/Linux run `scripts/launch_selector.sh --timeout 900`. It loads the persisted subscription automatically. Keep the same agent turn active while the user uses the browser window. Run the bridge in the foreground as a blocking call with a timeout of at least 900 seconds; process exit means the user has submitted. As soon as it returns, immediately post one short confirmation in the conversation (for example `已收到你的选择，正在创建订阅任务…`), then continue parsing and execution in the same turn without asking for another confirmation. If the host can only run the command in the background, poll its output at least every 5 seconds and continue the instant `ZHIXUN_CONFIG=` appears. Never end the turn while the bridge is still running; do not ask the user to notify you later as the primary path. Only when the host forcibly terminates the turn may the handoff pause — in that case tell the user to send `继续部署` and follow `Resume an interrupted handoff`.
5. Wait for both `ZHIXUN_CONFIG=` and `ZHIXUN_AUTOMATION_REQUEST=`. Both values are ASCII-escaped JSON (`ensure_ascii`), so they parse losslessly under any console encoding; decode them with a standard JSON parser. Parse the JSON values, validate the configuration, and immediately execute `Create the recurring task`. The selector step is not complete until the automation is created and verified.
6. If the script prints `ZHIXUN_SELECTOR_URL=`, show that URL only when the browser did not open automatically. If Python or local HTTP serving is unavailable, fall back to opening `assets/browser-subscription-selector.html`; only in this degraded path may the user need to copy the creation request.
7. Never claim that opening, saving, or submitting the selector creates a scheduled task. A subscription is active only after the host returns a task identifier or the task appears as enabled in its automation list.

## Text selector for headless or cloud hosts

Determine capability, not physical location. Treat the host as unable to present the interactive selector when it has no inline HTML callback, no user-accessible graphical browser, is running through a headless SSH/container/server session, or the loopback selector URL cannot be reached by the user. These are strong signals of a cloud/headless Agent, but do not claim certainty about where the machine is physically hosted. If interactive capability is already known to be absent, enter this mode directly. Otherwise, enter it after the selector fails once for a capability or reachability reason; do not repeatedly retry the same unavailable UI.

In this mode, print the following complete list exactly as readable text. Do not shorten it, replace it with only recommended choices, or require the user to remember section names:

```text
当前环境无法向你展示可点击的交互界面，已切换为文字选择模式。
标准板块（可多选，请回复编号，例如：01、03、09）：
01：数科
02：寻源
03：撮合
04：用工
05：海外
06：高管观点
07：企业经营
08：投融资
09：AI
10：建筑软件
11：建筑科技
12：政府宏观
13：行业数据
14：标准规范
15：绿色低碳
16：拓展阅读

岗位画像：经营岗 / 生产岗 / 研究岗 / 自定义
相关度：可为每个已选板块填写 1–10；不填写则使用该岗位预设。
还可以补充自定义关注方向，例如：城市更新、建筑机器人、基础设施REITs。
请按以下格式回复：
板块编号：01、03、09
岗位画像：生产岗
相关度覆盖：02=10，04=10，03=8
自定义关注：无（或填写具体内容）
频率：每天（或工作日）
上海时间：08:30
接收位置：当前平台（或填写宿主支持的位置）
```

Parse the reply into the same normalized configuration used by the graphical selector. Allow all 16 standard sections, preserve the displayed numbering, validate with `scripts/validate_preferences.py`, and then continue directly to `Create the recurring task`. When editing an existing subscription, show the full list again, append `当前已选：...` and the saved cadence/time/channel, and use the reply as a complete replacement configuration. Do not create a duplicate task.

## Reopen and edit the subscription

When the user asks to change the time, cadence, standard sections, custom interests, item limit, or delivery channel:

1. Reopen the same interactive selector; do not make the user describe every setting again. On inline-visualization hosts, provide the normalized saved configuration through `window.__ZHIXUN_INITIAL_CONFIG__` so the Codex selector restores all prior values instead of resetting to defaults.
2. On a local host without inline HTML callbacks, run the selector bridge: on Windows run `scripts\launch_selector.cmd --edit --timeout 900`, on macOS/Linux run `scripts/launch_selector.sh --edit --timeout 900`. The script loads the last confirmed configuration from the user's local application-data directory and returns `ZHIXUN_SELECTOR_MODE=update`.
3. For the first upgrade from an older version, if no persisted configuration exists but an existing automation is readable, extract the normalized subscription JSON from its prompt into a temporary JSON file and run the same wrapper with `--initial-config-file <path> --timeout 900` (Windows: `scripts\launch_selector.cmd --initial-config-file <path> --timeout 900`; macOS/Linux: `scripts/launch_selector.sh --initial-config-file <path> --timeout 900`).
4. Keep `subscription_id` stable. Treat `primary` as the default subscription. Changing `delivery_time` must not change task identity.
5. After submission, update the existing task in place, replace its full configuration including `custom_interests`, re-enable it, recalculate the schedule contract, and verify the new actual `nextRunAt`.
6. If a legacy task has no `subscription_id` and exactly one task name begins with `知讯日报｜`, migrate that task to `subscription_id: primary` and the stable name `知讯日报｜主订阅`. Do not leave the old task or create a duplicate.

## Resume an interrupted handoff

Every selector submission is persisted by the bridge to a pending-deployment file next to the preferences file (Windows: `%LOCALAPPDATA%\zhixun-daily-brief\pending-deployment-<subscription_id>.json`; macOS/Linux: `~/.config/zhixun-daily-brief/pending-deployment-<subscription_id>.json`; the default `subscription_id` is `primary`). The bridge prints its absolute path as `ZHIXUN_PENDING_FILE=`. The edit flow for changing sections or delivery time shares the same bridge and handoff mechanism as first-time setup, so this section and the deployment-receipt requirement apply equally to edits.

When the user says `继续部署`, `我提交了`, `继续`, `部署呢`, or an equivalent resume request after submitting the selector:

1. Read the pending-deployment file for the subscription (default `primary`). If it exists and `consumed` is `false`, take `config` and `automation_request` directly from it, validate the configuration, and immediately execute `Create the recurring task`, including the `verify_schedule.py` check. Never make the user reselect.
2. After a successful deployment, set `consumed` to `true` in that file and keep the file as a record.
3. If the file is missing or already consumed, tell the user no pending submission was found and reopen the selector.

## Create the recurring task

When the configuration includes `create_schedule: true`:

1. Validate it with `scripts/validate_preferences.py`.
2. Before creating the task, run `python scripts/verify_schedule.py --cadence <cadence> --time <HH:MM> --timezone <timezone>`. Use the returned `schedule_contract` as the source of truth.
3. Check for an existing task by stable `subscription_id`, not by time-bearing task name. Update and re-enable it when found; otherwise migrate the single legacy 知讯日报 task or create one task. Immediately use the host's native scheduled-task or Automation action. On hosts that accept natural-language automation requests, execute the request returned as `ZHIXUN_AUTOMATION_REQUEST=`; do not merely display it and never leave a duplicate task.
4. Write all supported values into real scheduler fields: `cadence`, `wall_clock_time`, IANA `timezone`, and the absolute `expected_next_run_local` containing an explicit UTC offset. Prefer a structured local-time schedule. If the host requires Cron, use `cron_expression` together with `cron_timezone`. If it accepts a full RRULE, use the contract value containing `DTSTART;TZID=...`.
5. Codex local Automations may expose only a bare RRULE field. In that adapter only, submit the exact selected local wall-clock hour/minute and cadence (`BYDAY` for weekdays); never substitute the current time, a generated fallback time, or UTC components. Establish that the computer timezone is `Asia/Shanghai`, then verify both the persisted rule and the timezone-aware `nextRunAt`. If either cannot be established, fail closed instead of claiming deployment success.
6. Do not treat a time shown in the task name, prompt, selector, or rule label as proof of scheduling. Obtain both the host's actual `nextRunAt` and its persisted recurrence rule. For Codex, re-read the matching `~/.codex/automations/<task-id>/automation.toml` after the native update; use its `rrule` as the persisted rule and use the Automation record/database only read-only to obtain `nextRunAt`, status, and run history.
7. Verify both values in one command: `python scripts/verify_schedule.py --cadence <cadence> --time <HH:MM> --timezone <timezone> --now <contract-generation-time-if-known> --reported-next-run <nextRunAt> --reported-rrule <persisted-rule>`. Add `--require-timezone-aware-rrule` when the host claims to persist `DTSTART;TZID=...`. A matching `nextRunAt` alone is never sufficient; a nonzero exit means the deployment is wrong.
8. On mismatch, update the same task using the schedule contract, then freshly re-read both `nextRunAt` and the persisted rule and verify again. Retry at most twice. Do not create duplicates. If either value still mismatches, disable the faulty task and report the expected time, actual Shanghai time, persisted rule, and validation issues; never claim successful deployment. If a native host accepts content/status updates but leaves the old recurrence rule unchanged, treat schedule mutation as unsupported. Recreating the task changes its identity: do not delete/recreate automatically unless the user explicitly authorizes that migration.
9. After the task passes schedule verification, persist the exact effective configuration before declaring deployment complete: `python scripts/record_config_state.py --subscription-id <id> --state-dir .zhixun-state --config-file <normalized-config-or-pending-file> --effective-at <selector-submitted-at>`. The saved fingerprint intentionally includes cadence and delivery time as well as sections and custom interests, so any material same-day edit requires a fresh result.
10. Immediately after recording the configuration, run `scripts/check_catchup.py` in the same deployment turn. If the current Shanghai time has passed the newly selected time and the command exits `10`, generate the normal brief once immediately; never wait for tomorrow merely because the scheduler rolled its next occurrence forward. An HTML file or marker produced under a different configuration fingerprint must not suppress this run.
11. Put the normalized JSON in the scheduled prompt and explicitly invoke `$generate-daily-industry-brief` on every run. Require: a rolling 24-hour primary cutoff plus the per-section 24-to-48-hour supplement defined below, field-first A/B/C/D relevance, concentrated first-party sources, the runtime capability gate and fallback order in `references/runtime-compatibility.md`, the per-section coverage and candidate-level exclusion audit defined in `references/retrieval-audit.md`, both `scripts/validate_retrieval_ledger.py` and `scripts/validate_html.py` passing before success marking, configuration-fingerprint-aware success marking, a standalone HTML artifact named `daily-industry-brief-YYYY-MM-DD.html`, and its matching `retrieval-ledger-YYYY-MM-DD.json`.
12. Use the host's default task result channel unless `delivery_channel` requests another available connector. If a requested external channel is unavailable or unauthenticated, create the task with the default result channel and explain the remaining connector step.
13. Finish only after the task is enabled, the verified `nextRunAt` matches the contract within 60 seconds, the persisted recurrence rule matches the selected cadence/hour/minute, and the immediate same-day catch-up check has completed. Return the task identifier when exposed, selected sections, expected Shanghai time, actual Shanghai time, recurrence-rule status, catch-up status, and next run. Never invent a task identifier or time.
14. Deployment receipt is mandatory. Whether first-time creation or a section/time edit, once the automation is created or updated and verified, post a deployment receipt in the conversation within the same turn. It must include: the task name, `subscription_id`, cadence (工作日/每天), delivery time and timezone (`Asia/Shanghai`), the expected next run from the `schedule_contract`, the host's actual returned `nextRunAt`, whether the two match within the 60-second tolerance, whether the persisted recurrence rule matches, whether an immediate catch-up was required or completed, and the host task identifier when exposed. On verification failure or missing host capability, report honestly per the rules above; never fabricate success.
15. If the host exposes no Automation creation action, no actual next-run value, or no readable persisted recurrence/schedule listing, do not claim success. Return the prepared task name, schedule contract, and prompt, and state precisely which host field could not be created or verified.

## Handle an offline or missed local run

1. Never claim that a powered-off computer can execute a local automation. Local schedules trigger only while the relevant local host is available. Recommend the host's cloud or always-on scheduler (such as WorkBuddy Cloud, the operating system's Task Scheduler, or a cron daemon) when the brief must run at the exact wall-clock time regardless of the computer state.
2. Before any catch-up decision, require a current configuration state written by `scripts/record_config_state.py`. When `missed_run_policy` is `catch_up_same_day`, ask the host to attach a resume/startup/agent-launch trigger only if that trigger is actually supported. On each such trigger, run `python scripts/check_catchup.py --cadence <cadence> --time <HH:MM> --timezone <timezone> --subscription-id <id> --state-dir .zhixun-state --output-dir .`.
3. Exit code `10` means the current Shanghai date is scheduled, the wall-clock time has passed, and no valid success marker exists for the current configuration fingerprint. This includes a same-day HTML or marker created before a material settings change. Immediately run the normal brief once. Do not rewrite the windows to the missed scheduled time; calculate the 24-hour primary and 24-to-48-hour fallback windows from the actual catch-up run timestamp.
4. After a complete-coverage HTML artifact is fully written, run `python scripts/validate_retrieval_ledger.py <ledger> --expected-sections <count>` and `python scripts/validate_html.py <artifact> --ledger <ledger> ...`. Then run `python scripts/mark_success.py --subscription-id <id> --timezone <timezone> --state-dir .zhixun-state --html-file <artifact> --ledger-file <ledger>`. The helper reruns both gates and stores the configuration fingerprint only when both pass. If validation fails, any row is `observed`、`expanded`、`business-observation`、`limited` or `baseline`, or retrieval is unfinished, do not publish it as a completed brief and do not write a success marker.
5. Exit code `0` means no catch-up. Deduplicate by subscription ID, Shanghai local date, and current configuration fingerprint. Reopening the host repeatedly cannot duplicate the same configuration, while changing sections, interests, cadence, time, or another fingerprinted setting makes the same date eligible once more.
6. Treat legacy HTML files and markers without a fingerprint as valid only when no current configuration state exists. Once `current-config-<id>.json` exists, a missing or mismatched marker fingerprint is stale and must not suppress a catch-up.
7. If the host exposes neither a recovery trigger nor a cloud scheduler, explain that automatic open-computer catch-up cannot be guaranteed. The user can still type `补发今天的知讯日报`, which must run the same fingerprint-aware check. Deployment itself must still run the immediate check from `Create the recurring task`, even when no recovery trigger exists.

## Resolve preferences

1. Accept the selector's JSON configuration, a saved configuration path, or a natural-language list of sections. Ignore setup-only fields such as `create_schedule`, `delivery_channel`, and `host` when generating content.
2. Run `scripts/validate_preferences.py` when Python is available and a JSON configuration is available. Without Python, validate the same fields manually: known topic IDs, profile in `management/production/research/custom`, per-topic relevance integer 1–10, at least one selected or custom section, time format, and item ceiling 1–20. Record `manual-validation` in the audit.
3. Accept any combination of the 16 supported sections plus up to 20 non-empty `custom_interests`. Require at least one standard section or one custom interest. Treat every custom interest as a separate dynamic section; do not force it into the nearest standard section.
4. Preserve `user_profile`, `topic_relevance`, and `coverage_policy` in normalized configuration. Profile scores determine retrieval depth and source priority through `references/role-profiles.md`; they do not prevent retrieval for any user-selected section.
5. Treat `delivery_time` as scheduling metadata only. Do not claim this skill schedules itself.

## Collect current information

1. Determine the exact run timestamp in `timezone`, defaulting to `Asia/Shanghai`. Set `primary_cutoff = run timestamp - 24 hours` and `fallback_cutoff = run timestamp - 48 hours`. The fallback segment is `[fallback_cutoff, primary_cutoff)`; it is not a replacement for the primary window.
2. Read `references/topics.md` for the selected sections only, then read `references/role-profiles.md`, `references/coverage-continuity.md`, `references/section-source-catalog.md`, `references/discovery-source-ladder.md`, `references/source-map.md`, `references/retrieval-audit.md`, `references/anti-shortcut-execution.md`, `references/retrieval-routing.md`, `references/network-retrieval-playbook.md`, and `references/editorial-standard.md`; on a new host or when full search is unavailable also read `references/runtime-compatibility.md` before retrieval.
3. Before looking for news, complete the harmless capability probe in `references/network-retrieval-playbook.md`, write its result in `run.network_probe`, and select one usable primary transport. Do not treat an unavailable optional route as a retrieval error or keep retrying it. If any outbound web route works, do not ask the user to provide a feed and do not mark a section limited merely because `webSearch`/`webFetch` is absent. Create one coverage-ledger row for every selected standard section and every custom interest before issuing searches, plus one candidate-ledger record for every plausible page opened. Each included live candidate must record a direct record URL, canonical URL, source-family ID, event fingerprint, timestamp precision, time-basis type, and computed window class. Give every row field, source/actor and official-record passes before the construction/business-intersection pass. For broad rows, the first three passes must omit AEC terms. A global construction-news query does not count as coverage for all rows, and a construction-only query cannot prove a broad adjacent field empty.
4. Select the highest supported retrieval mode from `references/runtime-compatibility.md`: Mode A search plus direct-page verification; Mode B any confirmed direct-network route (browser navigation, browser search page, shell/HTTP request, RSS, sitemap, public listing/API or site-native search); Mode C a current structured JSON feed that passes `scripts/validate_news_input.py`; or Mode D explicitly limited/offline output. Execute the adaptive finding and evidence-based empty-section checks in `references/retrieval-audit.md`, the mandatory macro/industry/project candidate comparison and management-evidence-chain composition in `references/macro-editorial-architecture.md`, use the A/B/C/D admission policy in `references/topics.md`, and follow the mandatory event-level route ladder in `references/retrieval-routing.md`. Separate discovery from verification: for every high-output or 7–10 relevance row, search both Tier 1 families and the relevant Chinese financial/national/vertical/announcement-mirror discovery pool in `references/discovery-source-ladder.md` before choosing cards; do not treat an official listing as the sole discovery route. Compare the fresh candidates found across both lanes and retain the strongest non-duplicate events, rather than stopping at the first valid official item. Then verify each event with the closest accessible evidence. After final event assignment, form 2–3 reader-facing evidence chains only from cards whose facts establish a genuine policy/制度、industry/资本、enterprise/project relationship; use an explicit partial-chain label when a layer is absent, rather than inventing a national-level conclusion. An established Tier 2 page may be the reader-facing citation for an ordinary event when it names the underlying actor/record, supplies a qualifying date, and is labeled `T2权威转述`; material or disputed claims still require an official record or two independent credible sources. For normal live runs, never reuse a previous HTML, prior ledger or cached example as the candidate pool: those files may be used only as a lead list and every retained item must be rediscovered and re-verified against this run's 24/48-hour window. A successful zero-result query or a current official listing whose newest entry is outside the window is valid coverage evidence; it is not a retrieval failure.
5. A section with verified qualifying items in the primary window is `complete` once it meets the finding threshold; do not require the broader quietness threshold merely to retain valid news. When a section has no qualifying primary-window item and its required query plus official-source check both executed successfully, treat the primary window as checked-empty and search the fallback segment from 24 to 48 hours ago. Include qualifying fallback items with an explicit `48小时补充` label. Do not mix fallback items into a section that already has a qualifying primary-window item. Retry only a path that failed; mark `limited` only after that failed path remains unavailable, never merely because the section returned zero candidates.
6. Include an item only when its original publication timestamp or the underlying event timestamp is within `[fallback_cutoff, run timestamp]`, then classify it as `primary` when it falls in `[primary_cutoff, run timestamp]` or `fallback` when it falls in `[fallback_cutoff, primary_cutoff)`. A fresh repost of an older event does not qualify. Preserve publication time and event time separately whenever an older notice announces a current event; the visible source line must never relabel an old publication date as the current event date. For a date-only page on a window-boundary date, first seek metadata, feed time, exchange record, attachment or underlying event time. If none is available but the page is authoritative or a qualifying Tier 2 account and its stated local date is demonstrably inside the overall rolling 48-hour range, render it only as a separate `48小时窗口观察` card: state that the source did not disclose a time of day, do not call it 24小时 or 48小时补充, give the row status `observed`, and do not count it as complete coverage or write a success marker. A date-only candidate must never be rendered as `primary`, `fallback`, `expanded`, or `business-observation`. Exclude a date-only item whenever even its calendar date cannot be shown to fall inside the 48-hour range. Apply the section-level fallback gate before rendering any `fallback` item.
7. Never widen beyond 48 hours to 72 hours or seven days, including weekends and quiet news days. The reader-facing page must not end in an empty section: after the strict levels are exhausted, use the evidence-backed expanded levels in `references/coverage-continuity.md` rather than filling with stale news, anonymous examples or invented candidates.
8. Open every plausible candidate through at least one accessible evidence route. Confirm the title, qualifying publication/event timestamp, core fact, publisher, and a direct record URL rather than a generic agency/listing/search page. A card whose text says that the key record, amount, counterparty, speaker, or result still needs verification is not a formal realtime card; continue verification or label it transparently as an observation. Failure of the first URL is a route failure, not an item failure: continue through the official alternate endpoint, event counterparty/registry, and qualifying Tier 2 routes in `references/retrieval-routing.md` before excluding it.
9. Exclude duplicate syndications, pure opinion without a new event, clickbait, unverified claims, and undated pages. Create one normalized event fingerprint from actor, event/action, affected object, event date and canonical URL before assigning sections. Assign every qualifying event to exactly one selected section: choose the row with the strongest direct field match, then the strongest decision impact, then the user's relevance score; record other possible rows only as excluded `assigned-to-<section-id>` leads. Never render the same event, canonical URL, announcement or source item twice across the brief, including as `跨板块复用` or `关联资讯`; different mirror URLs for the same procurement, filing or event still count as one event. A custom interest does not automatically outrank a standard section; the event belongs to whichever selected row is most directly about it.
10. Keep at most `max_items_per_topic` full section cards per section, defaulting to twenty and never exceeding twenty. This is a ceiling, not a reason to stop after one item. The rendered count is **not** a quota: one independently qualifying card can be valid, but only after the required candidate depth has been completed and documented. Set a **candidate-screening target** of two independent non-duplicate cards for every selected row and three for high-output or 9–10 relevance rows; `target_card_count: 1` is invalid for a live run. For every ordinary selected row, create at least **four** candidate-ledger records across at least **two** independently maintained source families. For every high-output, 7–10 relevance, or broad custom-interest row, create at least **six** candidate-ledger records across at least **three** independently maintained source families. These are review-depth floors, not a license to add weak stories. In every required discovery lane, first screen all current candidate entries for title, time, source, apparent event identity and direct section relevance. Then open and verify every non-duplicate plausible candidate; write an included or exact excluded decision for each. Include every candidate that independently passes time, source, relevance and de-duplication checks, up to `max_items_per_topic`. Do not stop after the first, second, or third qualifying item. Finalize only after the required lanes and their already discovered candidate pool are exhausted with a concrete exclusion reason for every non-included candidate, or after the configured item ceiling is reached. For every high-output or 7–10 relevance row, the retained/compared candidate pool must include a macro-or-industry route and a transaction/project/company route whenever current eligible material exists; a project-only row cannot be called editorially complete. If fewer than the target qualify, complete the additional discovery pass, retain every rejected candidate with its exact exclusion reason, and record a specific `below_target_reason`. A section with one valid full card may still be `complete` only after this evidence-based review. For every selected high-output or 7–10 relevance row, a live-search host must attempt the national/statutory lane and a different official, vertical or disclosure-mirror lane before finalizing. Use Tier 2 authority/industry/financial reporting as a discovery and ordinary-event completion lane when it identifies the actor, underlying record and date; label it faithfully rather than discarding it merely because a first-party page is slow or unavailable. Rank by exact section relevance, source authority, freshness, decision value and the profile's source priority; use alignment with `industry_scope` as a ranking bonus rather than a universal filter. Before writing, run the source-diversity checks in `references/retrieval-audit.md`; do not let convenience mirrors or one disclosure portal dominate the brief when equivalent independent sources are available.
11. For `leadership`, verify the speaker's current role from an official source and capture the occasion, event timestamp, core argument, and business implication. Prefer a faithful paraphrase; use only a short direct quote when wording is material.
12. For `fintech`, follow the dedicated financial-services and data-tech watchlist in `references/source-map.md` and `references/section-source-catalog.md`. Search regulation, products, enterprise customers, transactions, partnerships, operating results and risks across B2B/industrial financial technology. Prefer AEC and industrial-chain implications, but do not reject a valid fresh sector event solely because it lacks a construction customer. Exclude consumer promotions, lifestyle finance and generic technology publicity with no financial/data-platform implication.
13. For `digital`, collect AI news only. Require AI capability, product, model, agent, compute/data infrastructure, governance or meaningful adoption to be the core novelty. Search broad AI events as well as AEC/industrial adoption. Route non-AI software and digital-twin news to `informatization`, physical intelligent-construction technology to `construction-tech`, and financial data-platform news to `fintech`.
14. For each `custom_interests` value, derive field, actor/product, event-verb, regulator/registry, transaction and business-intersection cues from the exact phrase, search it as an independent broad section, and apply the same 24-hour-primary/48-hour-fallback, Tier 1-first, candidate-ledger, and `max_items_per_topic` rules. Keep the user's exact label as the section title. Treat the custom field itself as sufficient scope unless the user explicitly narrows it; use the configured construction/AEC ecosystem only to rank and explain items. For every custom row, execute four mandatory lanes: exact-field events; named sources/actors; official/regulatory/registry/project records; and business/AEC intersections. If zero or one full card qualifies, run one additional national/vertical/mirror discovery pass before finalizing. A new label does not need a predefined profile; derive the taxonomy dynamically. For example, `游戏` expands to approvals, publishers, launches, operations, investment, overseas and regulation, while `外贸` expands to customs, orders, exhibitions, trade policy, logistics, finance and company operations.
15. Route at least one directly relevant qualifying event to each custom-interest section as a full card whenever such an event exists. If the same event also has A/B/C-level value for a standard section, assign it only to the single strongest matching selected section and preserve the other row's lead as excluded `assigned-to-<section-id>` evidence. If no primary-window full card qualifies after the mandatory lanes, complete the row's 24-to-48-hour fallback search. If none qualifies there either, first render one real `扩展相关资讯` C-level card; if that remains unavailable, render one real `业务观察` card from a current official statistic, public project/procurement record, authoritative research, association release or established vertical/financial media. Do not use D-level speculation, stale news, anonymous candidates or invented examples. The expanded levels do not count as strict complete coverage or authorize a success marker.
16. Before writing, reconcile ledger totals: every selected section must be `complete`, `observed`, `expanded`, `business-observation`, `checked-empty`, `limited`, or, only for an offline Mode D run, `baseline`; `candidate_count` must exactly equal the number of candidate-ledger rows for that section; independent and cross-section card counts must equal rendered full cards; shared event IDs must reconcile to unique-event counts; compact related counts must equal rendered `关联资讯` cards; excluded counts must equal candidate-ledger reason totals; and final source-domain counts must pass the diversity audit or carry a specific exception. Run the anti-shortcut contract in `references/anti-shortcut-execution.md`: a live-capable run without section-level retrieval proof, an under-target row without the required review-depth floor, documented additional pass and `below_target_reason`, a false candidate count, or any live row downgraded to `baseline`, fails validation. Do not publish a partial-looking HTML after such a failure. Do not use a one-off renderer, hand-built HTML, or a prior HTML artifact to bypass the paired live candidate ledger → ledger validator → renderer → HTML validator chain. Do not leave a completed row visually blank: apply the five-level continuity ladder using real verified material. Every included or excluded candidate must carry a real title, source name, direct HTTP(S) URL and date/time basis; anonymous labels such as “某软件厂商” or synthetic URLs are invalid. If any row is missing, a row is compact-related-only, a broad field was searched only through AEC intersections, or a candidate was dropped only because its first URL failed, continue the applicable discovery or alternate route rather than silently producing an unreviewed or empty section.

## Write the HTML brief

### Editorial quality and layout gate

Use the supplied `daily-brief-template.html` as an intact application shell. Do not hand-build a reduced header, audit table, search script, or the `management-map` recommendation component. Before delivery, inspect the generated page at desktop width and mobile width when the host can render it; a structurally valid HTML file whose cards, audit rows, recommendations, or header have visibly collapsed is not acceptable.

Every full card is a leadership brief, not a search-result rewrite. Keep the existing fields `发生了什么`、`为何值得关注`、`行动/风险点` and `研判` unchanged, but write substantive prose after each label. `发生了什么` must identify the actor, dated action and at least two concrete decision facts whenever the source supplies them (for example amount, scale, place, project stage, product capability, contract term, counterparty or policy mechanism), normally in two or more complete sentences. `为何值得关注` must use those facts to explain the selected board's opportunity, cost, risk, competition, compliance or customer decision; it must state the relevant downstream scope rather than merely say “值得关注”. `行动/风险点` must name a specific next check, decision boundary or delivery risk. `研判` must state a bounded implication and the next observable proof point. Do not pad thin sources with imagined facts, but do not compress a well-documented source into a caption. Do not use retrieval-process language, generic “持续关注” wording, or a statement about timestamp precision as a `今日信号`.

Before drafting a full card, extract the source's actor, date, event stage, affected object and every available numeric/operational fact. A formal card normally needs at least two such facts, or one fact plus a concrete stage, mechanism or counterparty. If the direct page is too thin, follow the source's announcement, procurement file, company filing, regulator record or credible named report before finalizing; do not compensate with generic analysis. Write the fact paragraph as a coherent management briefing with enough context to stand alone, not a one-sentence caption. A source that genuinely contains only a thin fact may still be a transparent observation, but it must not be selected as a highlight over a better documented candidate.

The three `今日重点` cards must be genuine primary-window stories. On desktop, use the template's compact leadership header: brand at left, a broad three-card focus strip in the centre, and search at right, aligned as a single three-column composition. The `今日重点` label is centred above its three-card strip. In the search column, lower the complete search block so that the top of the input aligns with the top of the three focus cards; keep its label directly above the input and the result status directly beneath. The focus-card list must explicitly reset its ordered-list margin, padding and markers so no default `1./2./3.` can misalign the cards. Each card uses a short business-type label plus a specific decision fact; give the centre strip enough width for normal Chinese line wrapping, reduce header height before narrowing cards, and never concatenate a broad topic name and a vague slogan. The search label and result-status text must remain legible (desktop labels at least 14px); their combined vertical rhythm should visually match the focus-card block without adding unused header space. Before sections, render the light `management-map` as `今日推荐关注`: two or three themes, each using the retained three-segment card layout to show parallel, independently verified evidence. Use factual segment labels such as `公开信息`、`行业/场景`、`企业/项目`; retain the bordered cards, pale backgrounds and three-column desktop grid rather than falling back to a plain numbered list. A short `核验重点` may follow the three cards only when it is a concrete check derived from those cards. Do not display arrows, words such as “导致/因此/从…走向…”, a generic management action, or an unsupported causal conclusion. A section with only a baseline, limited, observed, expanded or business-observation card must not enter 今日推荐关注. `管理层动作清单` is a management synthesis, not a dark technical callout: use the template's light card treatment, no browser-default blue links, and two or three substantive cross-section conclusions. Each conclusion must name the relevant facts or numbers, state the resulting opportunity/risk, and give the next management observation or action; it must never summarize validation status, missing timestamps, retrieval routes or other production metadata.

The reader-facing `检索审计` is an accountability panel, not a narrative block. It must use exactly seven aligned columns (板块、来源族、候选、独立卡、跨板块、紧凑关联、状态), exactly one row per selected section, and a populated disclosure below each row. Build each data row as one `data-audit-row` wrapper containing exactly seven `span` cells; preserve the template's `audit-grid` and `audit-body` classes. Never render section detail disclosures inside the seven-column grid, never concatenate labels and counts, and never replace numeric cells with explanatory prose.

1. Copy and adapt `assets/daily-brief-template.html` as the visual and semantic contract; do not reconstruct a simplified page from memory. Create a complete HTML5 document with embedded CSS and only the template's embedded search/navigation JavaScript; do not add external JavaScript, fonts, trackers, or remote assets. The generated file must retain the template's `.highlights-label` and the grouped `.highlights` card structure; a hand-written replacement header is invalid.
2. Save the final artifact as `daily-industry-brief-YYYY-MM-DD.html` in the current task or automation workspace. Never wrap the HTML in a Markdown code fence and never substitute a Markdown document.
3. Include the title, run timestamp and timezone, exact rolling 24-hour primary window, the 24-to-48-hour fallback segment, selected standard sections and custom interests, unique-event count, full section-card count, cross-section-card count, compact related count, fallback-card count, estimated reading time, coverage completion count, a persistent left navigation pane, and one section per selected or custom topic. The desktop navigation must begin with `今日推荐关注`, then use first-level groups with second-level topic links: `业务与市场`（数科、寻源、撮合、用工、海外、高管观点、企业经营、投融资）, `技术与生态`（AI、建筑软件、建筑科技）, `政策与行业`（政府宏观、行业数据、标准规范、绿色低碳、拓展阅读）, and `自定义关注`（the exact selected custom labels). Omit an empty group; on mobile retain compact horizontally scrollable second-level topic links.
4. For each full card include: title, `发生了什么`, section-specific `为何值得关注`, `行动/风险点`, `研判`, source name with a clickable direct-record link, and exact publication/event time when available. Apply the editorial quality and layout gate above. When an event date rather than publication date is used for the time window, render both separately and label the time basis; never conceal an older publication date. If the page cannot support the required factual fields, keep it as a candidate rather than padding a full card. Give every card a unique HTML `id`, a unique stable `data-event-id`, the truthful `data-entry-kind`, and a `data-story-section` carrying its section name for the left/mobile badge. Open every external source with `target="_blank" rel="noopener noreferrer"`. Mark the brief's inference with `研判`.
5. For `leadership`, use `核心观点` and also include `发言人`, verified current role, `场合`, and event time. Keep the speaker's view distinct from confirmed facts and from `研判`.
6. Replace the plain selected-topic subtitle under `知讯日报` with a compact `今日重点 · 按业务类型` card group: select exactly three independent high-value facts whenever at least three strictly verified primary-window cards exist, label each with a concise business type such as `项目机会`, `企业经营`, `政策监管`, `技术产品`, or `资本市场`, and link it to that story's HTML `id`. Never select a `48小时补充`、`48小时窗口观察`、`扩展相关资讯`、`业务观察` or `基线追踪` card as 今日重点. Keep the label visible above the three cards, use the template's bordered cards, and do not leave unused green space below the group. Render `今日推荐关注` after the search/no-results area and before section cards; each recommendation retains three equal, source-linked segments that are read as parallel facts, not a causal chain. Include only formal source-backed themes and no more recommendations than the evidence supports. Then retain `管理层动作清单`, summarizing two or three cross-section decisions.
7. Render every standard and custom row with a truthful `data-coverage-status`; custom interests keep their exact label and `data-section-kind="custom"`. A row marked `complete` needs at least one independently verified full `.story` card; no cross-section or compact-related cards are permitted. Its review target remains two or three cards, but it may legitimately contain only one when the mandatory expansion pass was completed and the audit gives the specific `below_target_reason` plus each rejected candidate's reason. Add later candidates only when they independently pass the admission checks. Mark every fallback full card visibly as `48小时补充`, show its exact timestamp, and set the section window label accordingly. A date-only boundary item uses `data-entry-kind="date-observation"`, `data-freshness-window="observed"`, and a visible `48小时窗口观察 · 原始页面未披露时分` badge; its row status is `observed`, it must not raise `{{COVERAGE_COMPLETE}}`, and it blocks `mark_success.py`. If strict levels have no card, render `data-entry-kind="expanded"` with a visible `扩展相关资讯` badge for a genuine C-level current item; otherwise render `data-entry-kind="business-observation"` with a visible `业务观察` badge for a real current statistic, project/procurement record, authoritative research or established vertical/financial-media report. Both statuses block `mark_success.py`. Only when no live information source is available may a `.baseline-card` be used. For a `limited` section, use the same baseline card but state `实时检索能力不足` rather than pretending it is a complete search.
8. Preserve the template's application-shell behavior: the top information/search area and left section navigation stay fixed on desktop while only the article pane scrolls. On mobile and embedded browsers, measure the rendered topbar with `ResizeObserver`, place the horizontal section tabs immediately below it using `--mobile-topbar-height`, account for safe-area insets, and never use a hard-coded sticky offset. Keep the keyword search functional across story titles, summaries, relevance, judgments, sources, section names, checked-empty cards, and limited-coverage cards; highlight matched keywords in visible content with `<mark class="search-hit">`. Pressing `Enter` in a non-empty search box must smoothly jump to and focus the first visible matching `.story`; `Escape` clears the search. Do not auto-scroll while the user is still typing. Keep the document responsive, accessible, print-friendly, and fully usable offline.
9. After `今日信号`, include the template's collapsible `检索审计` panel. It is a required reader-facing deliverable, not an optional developer note: retain the template's audit summary, grid header and exactly one `data-audit-row` per selected section. Every row must show section label, independently maintained source-family count, candidates opened, independent cards, cross-section cards, compact related cards and status. Under each row, render the actual checked source families (named channel plus domain), included card title(s), direct time basis, and concise pool-closure reason; for every under-target, checked-empty, limited, observed, expanded or business-observation row also show up to three real excluded candidate summaries from the validated ledger. Do not count a domain and its publisher name twice, and never call `WebSearch 混合来源结果` a source family. Do not replace this panel with a one-line “试运行”, “已检索” or generic explanation, and do not render an empty details element. Keep raw query strings out of the reader-facing HTML unless needed to explain a limitation.
10. When Python is available, first run `python scripts/validate_retrieval_ledger.py retrieval-ledger-YYYY-MM-DD.json --expected-sections <selected-standard-plus-custom-count>`. Then run `python scripts/validate_html.py <artifact> --ledger retrieval-ledger-YYYY-MM-DD.json --expected-items <all-rendered-story-cards-including-baselines> --expected-sections <selected-standard-plus-custom-count> --expected-related <rendered-compact-related-count> --expected-unique-events <unique-event-count> --expected-cross-section <cross-section-card-count> --expected-baseline <baseline-card-count>`. The HTML validator auto-detects a same-date sibling ledger when `--ledger` is omitted, and fails when neither is present. Repair reported defects and rerun at most twice. Without Python, run the same anti-shortcut checks manually and label the audit `manual-validation`; this must not be falsely described as script validation. A nonzero validator result blocks the success marker and any claim that a live search run completed. The HTML validator must reject related-only rows, `complete` rows without a full card, stale or date-only cards falsely labeled primary/fallback, duplicate events/canonical sources, source links that do not correspond to the validated ledger, or a normal report missing its ledger. Never deliver a file that lost the embedded search/highlight/navigation script, contains unresolved placeholders, reports counts that disagree with rendered content, or opens source links over the current brief.

11. **Do not hand-rewrite the template search JavaScript.** Copy the template's search/highlight/navigation script as a byte-preserving block; do not place its regular expressions inside a second language string literal that can collapse backslashes. After rendering, open the generated file in an available browser or browser-capable host, enter a Chinese term known to occur in a title or body, and verify all three outcomes: matching cards remain visible, at least one `<mark class="search-hit">` appears, and `#search-status` changes from the default total to a match count. If an interactive browser is unavailable, `validate_html.py` must pass and the delivery must say `静态校验，未完成浏览器交互验证`; it must not claim that search was tested.
11. In the host conversation, attach or link the actual standalone `.html` artifact—not a chat renderer's sanitized HTML preview—and give only a short delivery note. If the host preview strips JavaScript, say that search works after downloading and opening the attached file in a browser. If coverage is partial, say so explicitly and do not describe the run as successfully complete. Do not paste the entire brief as Markdown.

## Prepare recurring use

When the user wants daily delivery but has not explicitly authorized task creation, provide an automation-ready prompt containing the normalized JSON configuration and the explicit instruction to use `$generate-daily-industry-brief`. Ask for or retain the desired run time and timezone. Let the automation system own the schedule.

## Update preferences

When the user changes any preference, reopen the selector, restore both `topic_ids` and `custom_interests`, validate the complete replacement configuration, persist it, and update the task with the matching `subscription_id`. Do not create a duplicate task or retain removed custom interests.
