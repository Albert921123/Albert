interface:
  display_name: "建筑行业每日情报"
  short_description: "按岗位画像检索、跨Agent降级且不留视觉空白的HTML日报"
  default_prompt: "使用 $generate-daily-industry-brief 打开知讯日报设置；按宿主真实能力选择检索路径，并对每个已选板块执行同等深度的来源、主体和事件矩阵检索。找到一条不得提前停止；低于目标时必须更换关键词和来源族补检。只保留通过时窗、来源、相关性和去重核验的真实资讯；确实只有一条时保留一条，检索受限时如实说明，不得虚构新闻或绕过正式交付门。"
